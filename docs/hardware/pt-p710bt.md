# PT-P710BT hardware reference — moved

Canonical home is now the obwat package: `~/src/obwat/docs/hardware/pt-p710bt.md`
(along with `scripts/hardware-debug/`). This stub exists so older docs/plans
that link here don't dangle.
